Vagrant.configure("2") do |config|
# Setup Ubuntu version 18.04
  config.vm.box = "bento/ubuntu-18.04"

# Set CPU and RAM requirements
  config.vm.provider "virtualbox" do |v|
    v.memory = 3072
    v.cpus = 2
  end
end
